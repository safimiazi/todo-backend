services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Nh!1@aaa
      MYSQL_DATABASE: todo
      MYSQL_USER: root
      MYSQL_PASSWORD: Nh!1@aaa
    ports:
      - '3306:3306'
    volumes:
      - db-data:/var/lib/mysql

  backend:
    build: .
    depends_on:
      - db
    environment:
      DATABASE_URL: mysql://root:Nh!1@aaa@db:3306/todo
      JWT_SECRET: SJF30FIOLFJCILSwuwerioweofj
      EXPIRESIN: 12h
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules

volumes:
  db-data:
